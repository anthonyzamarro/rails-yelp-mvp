<!-- <div class="container">
  <div class="row">
    <div class="col-sm-12"> -->
      <h1><%= @restaurant.name %></h1>
      <h4>Category: <%= @restaurant.category %></h4>


      <p><strong>Address:</strong> <%= @restaurant.address %></p>
      <p><strong>Phone number:</strong> <%= @restaurant.phone_number %></p>

        <% @restaurant.reviews.each do |review| %>
          <p> User review: <%= review.content %></p>
          <p> User rating: <%= review.rating %></p>
        <% end %>


      <h1>Write a review</h1>
      <%= simple_form_for [@restaurant, @review] do |f| %>
        <%= f.input :content %>
        <%= f.input :rating %>
        <%= f.submit "add a review", class: "btn btn-primary" %>
      <% end %>



      <p><%= link_to "Go back", root_path %></p>
      <p><%= link_to "Click on review", new_restaurant_review_path(@restaurant) %></p>
      <p><%= link_to "Delete Restaurant", restaurant_path(@restaurant), method: :delete %></p>
  <!--   </div>
  </div>
</div> -->
