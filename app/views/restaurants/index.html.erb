<div class="container">
  <div class="row new">
    <div class="col-sm-12">
      <h2>Restaurants</h2>
        <ul>
        <% @restaurants.each do |restaurant| %>
          <li><%= link_to "#{restaurant.name}", restaurant_path(restaurant) %></li>
        <% end %>
        </ul>
        <button class="btn btn-info"><%= link_to "Add Restaurant", new_restaurant_path %></button>
    </div>
  </div>
</div>
